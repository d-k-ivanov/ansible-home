---
- name: Install software on Arch Linux
  include: pacman.yml
  when: 
    - ansible_os_family == "Archlinux"
    - ansible_pkg_mgr == "pacman"

- name: Install pip packages
  pip: name={{ item }} state=present
  when: need_software_setup is defined
  with_items:
    - "{{ pip_packages }}"